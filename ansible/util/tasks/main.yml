---
- name: Install general utilities
  command: "{{ home_dir }}/macfiles/bin/brew-install-or-upgrade {{item}}"
  with_items:
    - colordiff
    - gh
    - git
    - htop
    - ripgrep
    - stern
    - tldr
    - tree
    - watch

# REPLACE WITH A TEMPLATE
# - name: Create link for ~/.editorconfig
#   file:
#     src: "{{ macfiles_dir }}/.editorconfig"
#     dest: "{{ home_dir }}/.editorconfig"
#     state: link
